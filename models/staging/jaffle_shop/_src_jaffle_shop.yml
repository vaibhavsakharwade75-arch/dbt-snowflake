sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    config:
      freshness: 
        error_after:
          count: 1
          period: day
        warn_after:
          count: 12
          period: hour
        loaded_at_field: __etl_loaded_at  
    tables:
      - name: customers
        freshness: null
      - name: orders

        